export const DEFAULT_LOCATION = localStorage.getItem('location') || 'london';

export const INITIAL_LANG = localStorage.getItem('lang') || navigator.language.slice(0, 2) || 'en';

export const INITIAL_THEME = localStorage.getItem('theme') || 'light'

export const CLASS_HIDDEN = 'hidden'; 

export const CLASS_UNVISIBILITY = 'unvisibility';

export const CLASS_ACTIVE = 'active';

export const CLASS_THEME_DARK = "theme-dark";

export const CLASS_SKELETON_LOADING = 'skeleton-loading'
